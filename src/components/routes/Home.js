import React from 'react'
import TextBox from '../shared/TextBox'
// import styled from 'styled-components'

const text = 'Switching SEIdes'

const Home = (props) => {
  return (
    <React.Fragment>
      <TextBox text={text} />
    </React.Fragment>
  )
}

export default Home
